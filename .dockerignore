# Release image = API runtime closure for the PR Guardian wedge plus the
# control-plane worker (see Dockerfile, app/import_closure.py). remediation/ and
# resilience/ ship because the same image runs the opt-in Temporal remediation
# worker and the certification exercise suite; topology/ still does not, so the
# module that imports it stays out. tests/test_image_import_closure.py keeps
# this list honest.
product/incident_service.py
