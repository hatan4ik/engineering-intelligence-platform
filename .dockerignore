# Release image = API runtime closure for the PR Guardian wedge and the
# operational-intelligence triggers (see Dockerfile, app/import_closure.py).
# product/incident_service.py now ships: it is reached by app/operations_api.py
# and topology/ is part of the image closure. The two self-healing modules import
# the execution plane (remediation/) and stay out until that has a product
# decision. tests/test_image_import_closure.py keeps this list honest.
product/durable_self_healing.py
product/self_healing_service.py
